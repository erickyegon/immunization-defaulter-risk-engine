POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=immunization_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me
POSTGRES_SCHEMA=public
POSTGRES_SSL_MODE=prefer
DATA_TIMEZONE=Africa/Nairobi
MODEL_TIMEZONE=Africa/Nairobi
MLFLOW_TRACKING_URI=./mlruns
RANDOM_STATE=42
# API security — set a strong random value in production (e.g. openssl rand -hex 32)
API_KEY=
# Comma-separated allowed CORS origins; leave empty to allow localhost only
CORS_ORIGINS=http://localhost:3000,http://localhost:8080
